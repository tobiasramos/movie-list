import MovieCard from "../../movieCard/movieCard";
import MovieSearch from "../../movieSearch/movieSearch";

const Home = () => {
  return (
    <div>
      <MovieSearch />
      <MovieCard />
    </div>
  );
};

export default Home;
